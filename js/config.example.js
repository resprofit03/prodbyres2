/**
 * Скопируйте в js/config.js и подставьте значения из Supabase → Settings → API.
 * js/config.js в .gitignore — не коммитьте ключи в публичный репозиторий.
 */
window.PORTFOLIO_CONFIG = {
  supabaseUrl: "https://YOUR_PROJECT.supabase.co",
  supabaseAnonKey: "YOUR_ANON_KEY",
};
